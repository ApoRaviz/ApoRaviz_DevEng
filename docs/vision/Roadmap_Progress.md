# DevEng — Roadmap & Progress

> แยกออกมาจาก Project Vision & Learning Protocol — Discussion Summary (เดิม V0.0.8)
> **ไฟล์นี้คือไฟล์ที่จะถูกแก้บ่อยที่สุด** — อัปเดตทุกครั้งที่ทำ Step สำเร็จจริงระหว่าง Learning Loop
> อ้างอิงหลักการ/บริบทเต็มได้ที่ `Vision_Decisions.md` — สถานะคำถามค้างที่ `Open_Questions.md`
> อัปเดตล่าสุด: 23 กรกฎาคม 2026 — ✅ Step 1.1.3 ผ่าน Learning Loop และ Claude review (Independent Review/QA) ตามกติกา 2.4 แล้ว (Phase 0 จบครบ, Phase 1 เดินถึง 1.1.3); สร้าง `HealthController` (`GET /health` → `200 {"status":"ok"}`) ลงทะเบียนใน `HealthModule.controllers`, ถอด scaffold `AppController`/`AppService` + test เก่าออก, `AppModule` เหลือแค่ประกอบ `HealthModule`; เพิ่ม backend E2E (Jest+Supertest) เรียก `GET /health` และ `test/tsconfig.json` แยก type-check (ลบ `baseUrl` แทนการซ่อน deprecation); เพิ่ม Roadmap Step 1.1.8 Backend E2E Test (Jest+Supertest) แยกจาก 1.1.7 Unit Test และ 1.7.3 Playwright, Phase 1 ~41 loops, sync 1.1.7 เป็น Jest; Knowledge Sync: เพิ่ม `nestjs/concepts/controller.md` + อัปเดต commands/nest-cli-structure/nodejs version map; Step ถัดไปคือ 1.1.4 Service
> Date rule: ก่อนลง log หรือแก้ `อัปเดตล่าสุด` ต้องเช็กวันที่จริงของ session ก่อนเสมอ ห้ามเดาจาก log เก่า

---

## 7.1 ภาพรวม Phase

| Phase | เป้าหมาย | หมายเหตุ |
|---|---|---|
| **0. Foundation** | Setup Angular project, config, Git workflow | แตกละเอียดแล้ว — ดูข้อ 7.2 |
| **1. Walking Skeleton** | Angular ↔ NestJS ↔ Postgres ↔ Docker ↔ CI/CD + Auth Deep-dive | แตกละเอียดแล้ว — ดูข้อ 7.3 |
| **2. Core Feature** | DevEng MVP จริง (เสียบ AI แทน Stub) — รายละเอียดเต็มใน `Vision_Decisions.md` ข้อ 8 | Input validation, RBAC เบื้องต้น, Invite Token Gate |
| **3. Scale** | Queue ประมวลผล async (BullMQ) | Rate limiting, idempotency |
| **4. Storage & Audit** | เก็บไฟล์/ประวัติย้อนหลัง (MinIO) | Presigned URL หมดอายุ |
| **5. Automation** | Scheduled Tasks/Webhook | Webhook signature verification |
| **6. Smart Layer** | AI ขั้นสูง (Speech-to-Text, TTS, conversation) | Prompt injection awareness — ไม่รวม image/screenshot capture (ดู `Vision_Decisions.md` ข้อ 8.5) |
| **7. Production Hardening** | Security audit, Monitoring, Performance | HTTPS, dependency audit, load test |
| **8. Deployment & Architecture Survey** | PaaS, Kubernetes (minikube), Serverless, + Microservices concept | ความรู้เสริม ไม่กระทบ production จริง |

---

## 7.2 Phase 0 แบบแตกละเอียด

**0.1 Project Initialization & Tooling**
| Step | หัวข้อ |
|---|---|
| 0.1.1 | Node.js & npm คืออะไร ทำไมต้องเลือกเวอร์ชันให้ถูก |
| 0.1.2 | Angular CLI คืออะไร คำสั่ง `ng new` สร้างอะไรให้บ้าง |
| 0.1.3 | โครงสร้างโฟลเดอร์ที่ Angular generate มา — แต่ละไฟล์ทำหน้าที่อะไร |
| 0.1.4 | TypeScript คืออะไร ต่างจาก JavaScript ยังไง ทำไม Angular เลือกใช้ |

**0.2 Configuration Deep-dive**
| Step | หัวข้อ |
|---|---|
| 0.2.1 | `angular.json` คืออะไร แต่ละ key ทำหน้าที่อะไร |
| 0.2.2 | `tsconfig.json` คืออะไร strict mode สำคัญยังไง |
| 0.2.3 | `package.json` vs `package-lock.json` ต่างกันยังไง |
| 0.2.4 | Environment files คืออะไร |

**0.3 Styling Foundation**
| Step | หัวข้อ |
|---|---|
| 0.3.1 | ติดตั้ง Tailwind CSS เข้ากับ Angular — ทำงานยังไงเบื้องหลัง (PostCSS) |
| 0.3.2 | ไฟล์ config ของ Tailwind ตั้งค่าอะไรบ้าง |

**0.4 Git Workflow**

> สอนคู่กันทั้ง Git command line และ VS Code Source Control UI: ดู diff/stage/commit/undo/merge conflict ผ่าน IDE ก่อน แล้วเทียบกับ command เพื่อให้เข้าใจว่าปุ่มใน VS Code เรียกแนวคิด Git เดียวกัน

| Step | หัวข้อ |
|---|---|
| 0.4.1 | Git คืออะไร Repository คืออะไร |
| 0.4.2 | `.gitignore` คืออะไร ทำไมสำคัญ |
| 0.4.3 | Git working tree พื้นฐาน: `status`, `diff`, `add`, `commit`, `log` + VS Code Source Control |
| 0.4.4 | Branching Strategy คืออะไร แบบไหนเหมาะ solo dev |
| 0.4.5 | Merge คืออะไร และ merge conflict แก้อย่างไร ทั้ง command และ VS Code conflict editor |
| 0.4.6 | Undo ใน Git: `restore`, `restore --staged`, `commit --amend`, `revert`, `reset` ต่างกันยังไง + ปุ่มย้อนใน VS Code Source Control |
| 0.4.7 | Git safety tools ที่ใช้ทำงานจริง: `stash`, `reflog`, `cherry-pick`, อ่าน history ก่อนย้อน + เทียบกับ VS Code/GitLens ถ้ามี |
| 0.4.8 | Commit Message Convention คืออะไร |
| 0.4.9 | เชื่อม local repo กับ GitHub remote (`ApoRaviz_DevEng`) |
| 0.4.10 | Git daily workflow: `pull`, `push`, sync กับ remote, และเช็กก่อน push ทั้ง command และ IDE |

**0.5 Secret Management เบื้องต้น**
| Step | หัวข้อ |
|---|---|
| 0.5.1 | Environment Variable คืออะไร ทำไมไม่ hardcode secret |
| 0.5.2 | `.env` file คืออะไร ใช้ร่วมกับ `.gitignore` ยังไง |

---

## 7.3 Phase 1 แบบแตกละเอียด

**1.0 Design ก่อนเขียนโค้ด**
| Step | หัวข้อ |
|---|---|
| 1.0.1 | API Contract คืออะไร — ออกแบบ endpoint ก่อนเขียนโค้ด |
| 1.0.2 | Database Schema คืออะไร — ออกแบบตารางเบื้องต้น (ER Diagram ง่ายๆ) — ระบุ Primary Key / Foreign Key relationship ระหว่าง entity ตั้งแต่ขั้นออกแบบ — ตอนนี้มี Product Brief ชัดแล้ว ออกแบบ schema ได้ตรงกับ entities ใน `Vision_Decisions.md` ข้อ 8.7 |
| 1.0.3 | Sequence Diagram คืออะไร — วาด flow หลัก (register ด้วย token → login → พิมพ์ → AI แปล → เก็บผล) |
| 1.0.4 | Wireframe คืออะไร — ร่างหน้าตาคร่าวๆ ของหน้าที่ต้องสร้าง ก่อนลงมือเขียน component จริง |

**1.1 Backend โครงเปล่า**
| Step | หัวข้อ |
|---|---|
| 1.1.1 | Nest CLI คืออะไร โครงสร้าง project ที่ generate มา |
| 1.1.2 | Module คืออะไร |
| 1.1.3 | Controller คืออะไร — ทำ endpoint แรก (`/health`) |
| 1.1.4 | Service คืออะไร แยกหน้าที่จาก Controller ทำไม |
| 1.1.5 | Dependency Injection คืออะไร |
| 1.1.6 | Exception Filter คืออะไร — ทำไม error ดิบๆ (stack trace) ที่หลุดออกไปหา frontend คือ security leak, สร้าง Global Exception Filter แรก |
| 1.1.7 | Unit Test คืออะไร — เขียน unit test แรกให้ Service ด้วย Jest ตั้งแต่ skeleton ยังเล็ก (ฝึกนิสัยก่อนโค้ดจะเยอะ) |
| 1.1.8 | Backend E2E Test คืออะไร — ใช้ Jest + Supertest เรียก Nest application ผ่าน HTTP, เข้าใจ `test/` และ `jest-e2e.json`, และแยกจาก Unit Test กับ Playwright full E2E |

**1.2 Frontend คุยกับ Backend**
| Step | หัวข้อ |
|---|---|
| 1.2.1 | HttpClient คืออะไร เรียก API ยังไง |
| 1.2.2 | CORS คืออะไร ทำไม browser บล็อกถ้าไม่ตั้งค่า |
| 1.2.3 | Environment config คืออะไร ทำไมต้องแยก dev/prod |
| 1.2.4 | แสดงผลจาก backend บนจอ (data binding) — แทรก UI/UX fundamentals (spacing/typography/color) ตอนนี้ |

**1.3 ต่อฐานข้อมูลจริง**
| Step | หัวข้อ |
|---|---|
| 1.3.1 | PostgreSQL คืออะไร รันผ่าน Docker ครั้งแรก |
| 1.3.2 | ORM คืออะไร ทำไมไม่เขียน SQL ตรงทุกที่ |
| 1.3.3 | Migration คืออะไร ทำไมต้อง version control schema |
| 1.3.4 | DTO & Validation Pipe คืออะไร — ทำไมต้อง validate input ก่อนแตะ DB เลย, `class-validator` decorator ทำงานยังไงเบื้องหลัง |
| 1.3.5 | เขียน CRUD endpoint แรกต่อกับ DB จริง (ผ่าน DTO validation แล้ว) — endpoint นี้คือ endpoint แปลประโยคจริงตาม Product Brief ไม่ใช่ CRUD ปลอมฝึกมือ |
| 1.3.6 | Connection Pooling คืออะไร |
| 1.3.7 | Primary Key / Foreign Key **ระดับกลไกจริง** — DB บังคับ referential integrity ยังไง, ทำไม INSERT/DELETE ที่ละเมิด FK ถึงถูก reject, ผลกับการ JOIN ตาราง |
| 1.3.8 | Index คืออะไร — ทำไม Query เร็วขึ้น (แนวคิด B-Tree เบื้องหลัง), ใช้ `EXPLAIN ANALYZE` อ่านว่า Postgres planner เลือกใช้ index หรือไม่, Composite Index คืออะไร, ข้อเสีย/cost ของ Index ตอน insert-update, เทียบกับ Clustered/Non-clustered Index ที่เคยใช้ใน SQL Server |

**1.4 Docker Compose ห่อทั้งระบบ**
| Step | หัวข้อ |
|---|---|
| 1.4.1 | Dockerfile คืออะไร เขียนสำหรับ backend |
| 1.4.2 | Image vs Container ต่างกันยังไง |
| 1.4.3 | Docker Network คืออะไร |
| 1.4.4 | Volume คืออะไร |
| 1.4.5 | เขียน `docker-compose.yml` รวมทุกอย่าง |

**1.5 CI พื้นฐาน**
| Step | หัวข้อ |
|---|---|
| 1.5.1 | Continuous Integration คืออะไร |
| 1.5.2 | GitHub Actions คืออะไร workflow file หน้าตายังไง |
| 1.5.3 | ตั้ง pipeline รัน lint+test+build อัตโนมัติ |

✅ **ถึงจุดนี้ skeleton ทำงานครบ แต่ยังไม่มี auth**

**1.6 Auth/Security Deep-dive**

> เหตุผลที่อยู่ตำแหน่งนี้ (หลัง 1.1-1.5, ก่อน 1.7): เหมือนไม่ติดตั้งระบบล็อกประตูก่อนที่บ้านจะมีผนัง/หลังคายืนอยู่ได้จริง — ต้องมี skeleton เสถียรก่อน ค่อยติดตั้งระบบล็อกเข้ากับโครงที่มั่นคงแล้ว

| Step | หัวข้อ |
|---|---|
| 1.6.1 | JWT คืออะไร เขียนด้วยมือเอง (ไม่ใช้ library) |
| 1.6.2 | JWT เก็บไว้ฝั่ง Frontend ที่ไหน — `localStorage` vs `httpOnly Cookie`, trade-off ระหว่างความเสี่ยง XSS กับ CSRF, ต้อง LOCK ก่อนเขียน frontend auth จริง |
| 1.6.3 | Password Hashing คืออะไร ต่างจาก Encrypt ยังไง (bcrypt/argon2) |
| 1.6.4 | Passport.js คืออะไร ทำอะไรแทนที่เขียนมือไปบ้าง |
| 1.6.5 | OAuth2/OIDC concept (Authorization Code + PKCE) |
| 1.6.6 | Refresh Token Rotation คืออะไร |
| 1.6.7 | RBAC คืออะไร กำหนด role/permission เบื้องต้น — ใช้จริงกับ endpoint สร้าง Invite Token ที่ต้องเช็คสิทธิ์ admin (`Vision_Decisions.md` ข้อ 8.4) |
| 1.6.8 | Brute-force protection คืออะไร — concept การจำกัดจำนวนครั้ง login ผิด (implement เต็มรอ Rate limiting ใน Phase 3 แต่ต้องเข้าใจ concept ตั้งแต่ตอนนี้) |
| 1.6.9 | Frontend Auth Forms & State — สร้าง Login/Register form, validation, แยก form state, ใช้ Angular signal คุม auth mode และ class binding โดยยึด JWT storage decision จาก 1.6.2 |
| 1.6.10 | Sliding Auth Panel & Accessible Motion — ฝึก CSS transform/transition, ป้องกันการสลับซ้ำระหว่าง animation, focus management, responsive fallback และ `prefers-reduced-motion` |

**1.7 Stub AI Endpoint**
| Step | หัวข้อ |
|---|---|
| 1.7.1 | ออกแบบ endpoint ปลอม (`/translate`) ที่ต้อง login ก่อนเรียกได้ — ตอบกลับข้อความ hardcode เสมอ |
| 1.7.2 | ต่อ frontend → backend (มี auth) → ตอบกลับครบวงจร |
| 1.7.3 | E2E Test แรกด้วย Playwright — ทดสอบ flow เต็มจาก frontend (login) → backend → แสดงผล, ทำไม E2E test ต่างจาก Unit Test ตรงไหน (มองจาก user จริง vs มองจากโค้ดทีละชิ้น) |

**1.8 Deploy ขึ้น VPS จริง**
| Step | หัวข้อ |
|---|---|
| 1.8.1 | Reverse Proxy คืออะไร (Nginx/Caddy) |
| 1.8.2 | Deploy flow หน้าตาเป็นยังไง (build → push → run บน VPS) |

> รวม Phase 1 ทั้งหมดมีประมาณ 41 รอบ Learning Loop ย่อย

---

## Progress Log

> ส่วนนี้จะถูกเติมเข้ามาเรื่อยๆ เมื่อเริ่ม Phase 0 จริงหลัง 25/06/2026 — ใช้บันทึกว่า Step ไหนทำเสร็จแล้ว วันที่ และจุดติดขัดที่เจอจริง (ถ้ามี) เพื่อให้ Knowledge Sync (หลักการ 2.4) มีร่องรอยอ้างอิงย้อนหลังได้

- 2026-06-25 — ✅ Step 0.1.1 เริ่มและผ่าน Learning Loop แรก: แยกความหมาย Node.js/npm, ตรวจ `node -v`, `npm -v`, `node -p "process.versions"`, `which node`, `which npm`; ยืนยันเครื่องใช้ Node `v24.16.0` และ npm `11.13.0` จาก nvm path ชุดเดียวกัน; Knowledge Sync ร่างกลับเข้า `_docs/nodejs/teach/06-node-npm-version-check.md` แล้ว และผ่าน Claude review ตามกติกา 2.4 แล้ว (แก้คำว่า `server` ใน teach/06 ให้ตรงรูปแบบ และ header วันที่ใน Roadmap)
- 2026-06-25 — ✅ Step 0.1.2 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: อธิบาย Angular CLI กับ `ng new`, ตรวจ `npx ng version`, เจอ Node หลุดเป็น `v22.14.0` ตอนสร้าง project จึงใช้ `nvm use 24` กลับมาเป็น Node `v24.16.0` / npm `11.13.0`; สร้าง `ApoRaviz_DevEng` สำเร็จด้วย `npx -y @angular/cli@22.0.0 new ApoRaviz_DevEng --routing --style css --ssr --skip-git --package-manager npm`; อ่านไฟล์ที่ `ng new` สร้างแบบเป็นกลุ่ม ได้แก่ `package.json`, `angular.json`, `src/main.ts`, `src/index.html`, root component, routing, SSR files, `tsconfig*.json`, `.gitignore`, `.prettierrc`, `.editorconfig`; ตรวจ baseline ด้วย `npm run build` แล้วผ่าน มี output ที่ `dist/ApoRaviz_DevEng`; Knowledge Sync: command pattern มีอยู่แล้วใน `_docs/angular/commands.md`, เพิ่ม Node version recovery ด้วย `nvm use 24` เข้า `_docs/nodejs/commands.md` / `_docs/nodejs/teach/06-node-npm-version-check.md`, อุด gap เรื่อง `index.html` ↔ root selector ใน `_docs/angular/teach/angular-run-flow-and-angular-json.md` และ SSR รุ่นใหม่ใน `_docs/angular/teach/app-config-ssr-hydration.md`; note สำหรับ Step 0.1.3: ใช้ file map recap ก่อน Knowledge Check เพราะไฟล์ Angular scaffold เยอะ
- 2026-06-28 — ✅ Step 0.1.3 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: อ่านโครงสร้าง Angular scaffold ด้วย File Map Recap แทนการจำไฟล์ทีละตัว; แยก root files เป็นกลุ่ม project docs, npm/dependency, Angular/TypeScript config, source/assets, tooling/editor/git, generated/cache/output; แยก `src/app` เป็น root component family (`app.ts`, `app.html`, `app.css`, `app.spec.ts`) และ app setup family (`app.config*.ts`, `app.routes*.ts`); อธิบาย `src` vs `public`, `styles` vs `assets`, `node_modules` / `dist` / `.angular/cache` ว่าไม่ใช่ source of truth; ตรวจ scaffold ด้วย `npm test -- --watch=false` ผ่าน 1 test file / 2 tests; Knowledge Sync: เพิ่ม Angular Scaffold File Map และ `src/app` File Map ใน `_docs/angular/teach/angular-config-files.md`, และปรับ `_docs/TEACHING_RULES.md` เป็น File Map Rule เพื่อใช้กับบทเรียนที่มีไฟล์จำนวนมาก
- 2026-07-01 — ✅ Step 0.1.4 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: อธิบาย TypeScript ว่าเป็น JavaScript + type safety ตอนเขียน/build, อ่าน `app.ts` เพื่อแยก `@Component`, `protected readonly`, `signal`, ตรวจ `typescript` ใน `devDependencies`, อ่าน `tsconfig.json` เพื่อแยก `compilerOptions` กับ `angularCompilerOptions`, อธิบาย `readonly title = signal(...)` ว่าห้ามเปลี่ยนกล่องแต่ยัง `set()` ค่าข้างใน signal ได้, และอธิบาย build output ว่า browser/server รัน JavaScript output ไม่ได้รัน TypeScript source โดยตรง; Knowledge Sync draft: เพิ่ม `angular/concepts/typescript.md`, เพิ่ม `_docs/angular/teach/typescript-in-angular.md`, และปรับตัวอย่าง `tsconfig.json` ใน `_docs/angular/teach/angular-config-files.md` ให้ตรง Angular 22 scaffold จริง
- 2026-07-01 — ✅ Milestone: Phase 0.1 (Project Init & Tooling) ครบ 0.1.1–0.1.4 และผ่าน Claude review ตามกติกา 2.4 แล้ว
- 2026-07-01 — ✅ Step 0.2.1 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: อ่าน `ApoRaviz_DevEng/angular.json` เพื่อแยกบทบาท `angular.json` ว่าเป็น config ที่บอก Angular CLI ว่า project นี้ `build`, `serve`, `test` อย่างไร; แยก `architect` ว่าเป็นหมวด target หลัก เช่น `build`, `serve`, `test`; เข้าใจว่า `serve.defaultConfiguration` ใช้ `development`, `build.defaultConfiguration` ใช้ `production`, `build.options.browser` ชี้ `src/main.ts`, `styles` ชี้ global CSS, `assets` ชี้ static asset จาก `public`, `tsConfig` ชี้ `tsconfig.app.json`, และ SSR ใช้ `server` / `ssr.entry`; Knowledge Sync: ตรวจแล้วเนื้อหานี้มีอยู่ใน `_docs/angular/teach/angular-run-flow-and-angular-json.md` และ `_docs/angular/teach/angular-config-files.md` แล้ว จึงไม่เพิ่ม docs ใหม่
- 2026-07-03 — ✅ Step 0.2.2 ผ่าน Learning Loop, Claude review และ Claude follow-up review ตามกติกา 2.4 แล้ว: อ่าน `tsconfig.json`, `tsconfig.app.json`, และ `tsconfig.spec.json` เพื่อแยก `compilerOptions` ว่าเป็นกฎ TypeScript, `angularCompilerOptions` ว่าเป็นกฎเฉพาะ Angular, และ `references`/`extends` ว่าเชื่อม config แม่กับ config ย่อย; เข้าใจว่า `tsconfig.app.json` include `src/**/*.ts` แต่ exclude `src/**/*.spec.ts` เพราะไฟล์ test ไม่ควรเข้า app/production build; เข้าใจ strict/safety flags เช่น `noImplicitReturns`, `noFallthroughCasesInSwitch`, `noImplicitOverride`, `strictInjectionParameters`, `strictInputAccessModifiers` ว่าช่วยจับ bug ก่อน build/deploy; Knowledge Sync: เพิ่ม note เรื่อง glob pattern `*`/`**`, `.d.ts`, และ Angular schematic `strict` option vs TypeScript `"strict": true` / strict family / additional checks ใน `_docs/angular/teach/angular-config-files.md`; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0`; Reviewer แก้ parity ตัวอย่าง `tsconfig.spec.json` ใน `_docs` ให้มี `rootDir` ตรงไฟล์จริง; Codex ลบ trailing space ใน `ApoRaviz_DevEng/tsconfig.spec.json`; ยืนยันจาก `@schematics/angular` template ของ Angular CLI 22 ว่า schematic `strict` default เป็น `true` แต่ output `tsconfig.json` ไม่ใส่ TypeScript `"strict": true` แบบ umbrella
- 2026-07-04 — ✅ Step 0.2.3 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: อ่าน `package.json` และ `package-lock.json` เพื่อแยก `scripts`, `dependencies`, `devDependencies`, version range เช่น `^22.0.0` / `~6.0.2`, และ installed version จริงใน lockfile เช่น `@angular/core` `22.0.2` / `typescript` `6.0.3`; เข้าใจว่า `resolved` คือ download URL และ `integrity` คือ hash ตรวจความถูกต้องของ package; เข้าใจว่า `npm install` ใช้ตอนพัฒนา/เพิ่ม dependency ส่วน `npm ci` ใช้ติดตั้งตาม lockfile แบบตรงที่สุดใน CI และควร fail เมื่อ `package.json` กับ `package-lock.json` ไม่ตรงกัน; อธิบาย dependency cleanup rule ว่า `npm ci` ลบ `node_modules` แล้ว install ใหม่จาก lockfile ในคำสั่งเดียว และไม่ควรลบ `package-lock.json` เป็นท่าแรก ยกเว้นตั้งใจให้ npm resolve dependency graph ใหม่และพร้อม review diff + run build/test; Knowledge Sync: เพิ่ม `package-lock.json` section ใน `_docs/angular/teach/angular-config-files.md` และเพิ่ม lockfile cleanup warning ใน `_docs/angular/commands.md`; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0`; Reviewer verify installed version กับ lockfile จริง: `@angular/core 22.0.2`, `typescript 6.0.3`, และ `resolved`/`integrity` ตรงกับ docs; นำข้อเสนอไม่บล็อกไปปรับ wording ว่า `npm ci` ล้าง `node_modules` เองและ fail เมื่อ lockfile ไม่ sync
- 2026-07-04 — ✅ Step 0.2.4 ผ่าน Learning Loop และ Claude review (รวม re-review) ตามกติกา 2.4 แล้ว: ตรวจแล้ว `ApoRaviz_DevEng/src` ยังไม่มี environment files และ `angular.json` ยังไม่มี `fileReplacements`/`environment`; เข้าใจว่า Angular environment files ใช้แยก public config เช่น API base URL ระหว่าง dev/prod แต่ไม่ใช่ที่เก็บ secret เพราะค่าถูก build ไปอยู่ใน JavaScript ฝั่ง browser/production ได้; ตัดสินใจยังไม่สร้างไฟล์ตอนนี้ เพราะโปรเจกต์ยังไม่มี backend/API จริง และจะสร้างพร้อมใช้งานจริงใน Step 1.2.3; Knowledge Sync draft: เพิ่ม `_docs/angular/concepts/environment-files.md`, เพิ่ม concept index/sidebar, และเพิ่ม `Environment files` section ใน `_docs/angular/teach/angular-config-files.md`; Claude review พบข้อแก้บังคับเรื่อง layout/naming/ทิศทาง `fileReplacements` จึงปรับให้ตรง Angular 22: `environment.ts` เป็น base/default production, `environment.development.ts` เป็น development override, development configuration replace `environment.ts` ด้วย `environment.development.ts`, และเพิ่ม command `ng generate environments`; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0`
- 2026-07-06 — ✅ Step 0.3.1 ผ่าน Learning Loop และ Claude review (รวม re-review) ตามกติกา 2.4 แล้ว: ติดตั้ง Tailwind CSS v4 เข้ากับ `ApoRaviz_DevEng` ด้วย `npm install tailwindcss @tailwindcss/postcss postcss`; ตรวจ `package.json` แล้วมี `@tailwindcss/postcss` `^4.3.2`, `postcss` `^8.5.16`, `tailwindcss` `^4.3.2` และ `package-lock.json` lock dependency แล้ว; เพิ่ม `.postcssrc.json` เพื่อให้ PostCSS ใช้ plugin `@tailwindcss/postcss`; เพิ่ม `@import "tailwindcss";` ใน `src/styles.css`; เข้าใจ flow ว่า Angular อ่าน global CSS, PostCSS อ่าน `.postcssrc.json`, plugin แปลง Tailwind ให้เป็น CSS output ที่ browser ใช้ได้; ผู้เรียนรัน `npm run build` ผ่านแล้ว; ผู้เรียนตั้ง temporary visual marker `Tailwind OK` ด้วย Tailwind utility classes, เปิดหน้าเว็บเห็นผลจริง, ลบ marker ออกจาก `app.html` แล้ว และยืนยันขั้นตอนหลังลบเรียบร้อย; Codex re-run build ใน sandbox ติด `listen EPERM ::1` หลัง `Building...` จึงบันทึกว่าเป็นข้อจำกัด sandbox ไม่ใช่ Tailwind config error; Knowledge Sync: เพิ่ม note เรื่อง build check + visual check ใน `_docs/angular/teach/tailwind-css-v4.md`; Claude review พบข้อแก้บังคับว่า docs ทำ schematic command เป็นวิธีหลักทั้งที่ไม่มี schematic จริง จึงปรับให้ manual setup เป็นวิธีหลัก, ตัด schematic command ออกจากคำสั่งหลัก, เพิ่ม pipeline diagram `styles.css -> Angular build -> PostCSS -> @tailwindcss/postcss -> CSS output -> browser`, grep ยืนยันไม่เหลือคำสั่ง schematic ใน teach doc, และตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0`
- 2026-07-06 — ✅ Step 0.3.2 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: ตรวจ Tailwind config จริงของ `ApoRaviz_DevEng` แล้วพบว่ามี `.postcssrc.json` และ `src/styles.css` แต่ไม่มี `tailwind.config.js`/`tailwind.config.ts` ซึ่งไม่ผิดสำหรับ Tailwind CSS v4 setup นี้; เข้าใจว่า `.postcssrc.json` ตั้งค่า `plugins` ให้ PostCSS ใช้ `@tailwindcss/postcss`, และไฟล์นี้ถูกค้นพบตาม PostCSS config convention ไม่ต้องระบุ path ใน `angular.json`; ตรวจ `angular.json` แล้ว `styles` ชี้ `src/styles.css`; เข้าใจว่า `src/styles.css` ใช้ `@import "tailwindcss";` เพื่อเรียก Tailwind เข้า global CSS และอนาคตใช้ `@theme` ตั้ง theme token เช่นสี brand/font กลางได้; ตัดสินใจยังไม่ใส่ `@theme` จริง เพราะยังไม่มี design decision เรื่องหน้าตา/brand ของ DevEng; Knowledge Sync: เพิ่ม section เรื่อง Tailwind v4 config, PostCSS convention, `@theme`, และ design decision ใน `_docs/angular/teach/tailwind-css-v4.md`; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0`
- 2026-07-06 — ✅ Step 0.4.1 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: เริ่มจาก `git status` ใน `ApoRaviz_DevEng` แล้วพบ `fatal: not a git repository (or any of the parent directories): .git`; ตรวจ `ls -la | grep ".git"` เห็น `.gitignore` แต่ยังไม่มี `.git/`; รัน `git init` แล้ว Git สร้าง `/Users/aporaviz/ApoRaviz/ApoRaviz_DevEng/.git/`; อ่าน `git status` และ `git status --short` เพื่อเข้าใจ `No commits yet`, `Untracked files`, และ `??`; เข้าใจว่า `.git/` คือ database/สมุดประวัติของ repository ส่วน `.gitignore` คือรายการที่ Git ควรเมิน, `node_modules/` ไม่โผล่เพราะอยู่ใน `.gitignore`, และ VS Code Source Control แสดงสถานะเดียวกับ `git status`; Knowledge Sync: เพิ่ม `_docs/git/concepts/git-repository.md`, `_docs/git/concepts/index.md`, เพิ่ม `Init And First Status` ใน `_docs/git/commands.md`, และเพิ่ม Git Learning sidebar ใน `_docs/.vitepress/config.mts`; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0`; Claude review ผ่าน ไม่มีข้อแก้บังคับ
- 2026-07-07 — ✅ Step 0.4.2 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: อ่าน `ApoRaviz_DevEng/.gitignore` เพื่อแยกหมวด compiled output (`/dist`, `/tmp`, `/out-tsc`), Node (`/node_modules`), IDE/editor, VS Code, cache/misc, และ system files; ใช้ `git status --ignored --short` เห็น `!! .angular/`, `!! dist/`, `!! node_modules/` เพื่อเข้าใจว่า `!!` คือ ignored; ใช้ `git check-ignore -v node_modules` ได้ `.gitignore:10:/node_modules node_modules` และ `git check-ignore -v dist` ได้ `.gitignore:4:/dist dist` เพื่ออ่านว่า ignored จาก rule บรรทัดไหน; เข้าใจว่า `node_modules/` และ `dist/` ไม่ควร commit เพราะสร้างใหม่ได้จาก npm/build, `!` ใน `.gitignore` คือยกเลิก ignore เช่น `!.vscode/settings.json`, และ `.gitignore` คือไม่ track เข้า Git history ไม่ใช่แค่ไม่ push; Knowledge Sync: เพิ่ม `_docs/git/concepts/gitignore.md`, อัปเดต `_docs/git/concepts/index.md`, เพิ่ม `Ignore Files` ใน `_docs/git/commands.md`, และเพิ่ม `.gitignore` ใน Git Learning sidebar; Claude review ผ่าน ไม่มีข้อแก้บังคับ และนำข้อเสนอไม่บล็อกไปเสริม parent-directory gotcha ว่าถ้า ignore parent directory ทั้งก้อน เช่น `.vscode/` อาจ unignore ไฟล์ลูกไม่ได้ จึงใช้ pattern แบบ `.vscode/*` เมื่อต้อง allow บางไฟล์; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0`
- 2026-07-07 — ✅ Step 0.4.3 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: ฝึก Git working tree ด้วย `ApoRaviz_DevEng` repo จริง; อ่าน `git status --short --branch` ก่อน commit แรก เห็น `## No commits yet on main` และ `??` untracked files; ใช้ `git diff` แล้วว่างเพราะไฟล์ยัง untracked; stage `README.md` ด้วย `git add README.md` เห็น `A  README.md`; อ่าน `git diff --cached -- README.md`; เจอ pager `END` และเรียนว่ากด `q` เพื่อออก; ทดลอง `git restore --staged README.md` ก่อน commit แรกแล้วเจอ `fatal: could not resolve HEAD` เพราะยังไม่มี commit ให้ `HEAD` อ้างอิง จึงใช้ `git rm --cached README.md`; stage ทั้งโปรเจกต์ด้วย `git add .`; อ่าน `git diff --cached --stat`; commit แรกสำเร็จด้วย `git commit -m "Initial Angular project setup"` ได้ root commit `cf6a04b`; แก้ `README.md` เพิ่ม purpose แล้วอ่าน ` M README.md`, `git diff -- README.md`, `M  README.md`, `git diff --cached -- README.md`; หลังมี commit แรกแล้ว `git restore --staged README.md` ใช้ได้; commit ที่สองสำเร็จด้วย `git commit -m "Document DevEng learning purpose"` ได้ commit `c5ff988`; อ่าน `git log --oneline` เห็น `HEAD -> main` และตรวจ `git status --short --branch` กลับมา clean เป็น `## main`; Knowledge Sync: เพิ่ม `_docs/git/concepts/working-tree.md`, `_docs/git/concepts/staging-area.md`, `_docs/git/concepts/commit.md`, `_docs/git/concepts/head.md`, อัปเดต `_docs/git/concepts/index.md`, เพิ่ม section `Working Tree, Stage, Commit` ใน `_docs/git/commands.md`, และเพิ่ม concept pages ใน Git Learning sidebar; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`
- 2026-07-08 — ✅ Step 0.4.4 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: เริ่มจาก `ApoRaviz_DevEng` clean บน `main`; ดู `git branch` เห็น `* main`; สร้างและ switch เข้า branch ทดลองด้วย `git switch -c docs/branching-practice`; แก้ `README.md` เพิ่มบรรทัด branch practice; อ่าน `git status --short --branch` และ `git diff -- README.md`; commit บน branch ด้วย `git commit -m "Practice Git branching workflow"` ได้ commit `de0c78f`; ดู graph ด้วย `git log --oneline --graph --decorate --all`; switch กลับ `main` แล้วตรวจด้วย `sed -n '1,6p' README.md` เห็นว่าบรรทัดใหม่ยังไม่อยู่บน `main`; merge ด้วย `git merge docs/branching-practice` ได้ `Fast-forward` จาก `c5ff988..de0c78f`; ตรวจ graph เห็น `HEAD -> main` และ branch ทดลองชี้ commit เดียวกัน; ตรวจ README เห็นบรรทัดใหม่อยู่บน `main`; ลบ branch ที่ merge แล้วด้วย `git branch -d docs/branching-practice` และตรวจเหลือ `* main`, `git status --short --branch` เป็น `## main`; เข้าใจว่า `git merge X` คือเอา `X` เข้ามาหา branch ที่ยืนอยู่, ต้องยืน branch ปลายทางก่อน, VS Code Source Control ใช้เมนู `...` -> `Merge Branch...` ได้, และ prompt `Select a branch or tag to merge from` ให้เลือก branch ต้นทาง; Knowledge Sync: เพิ่ม `_docs/git/concepts/branch.md`, `_docs/git/concepts/merge.md`, อัปเดต `_docs/git/concepts/index.md`, เพิ่มตัวอย่าง branch/merge + VS Code wording ใน `_docs/git/commands.md`, และเพิ่ม Branch/Merge ใน Git Learning sidebar; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`
- 2026-07-08 — ✅ Step 0.4.5 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: ฝึก merge conflict จริงใน `ApoRaviz_DevEng`; เริ่มจาก `main` clean; สร้าง `docs/conflict-practice` ด้วย `git switch -c docs/conflict-practice`; แก้บรรทัด README เป็น feature branch version แล้ว commit `e0461f9` ด้วย `git commit -m "Add conflict practice branch version"`; switch กลับ `main`; แก้บรรทัดเดียวกันเป็น main branch version แล้ว commit `9690cff` ด้วย `git commit -m "Add conflict practice main version"`; merge ด้วย `git merge docs/conflict-practice` แล้วเกิด `CONFLICT (content): Merge conflict in README.md`; อ่าน `git status --short --branch` เห็น `UU README.md`; เปิด VS Code conflict editor แล้วเข้าใจ `<<<<<<< HEAD` คือฝั่ง `main`, incoming คือ `docs/conflict-practice`, `Accept Current Change`, `Accept Incoming Change`, และ `Accept Both Changes`; แก้ผลลัพธ์สุดท้ายให้ไม่มี marker แล้วใช้ `git add README.md` เพื่อ mark resolved; อ่าน `git status` เห็น `All conflicts fixed but you are still merging`; commit merge ผ่าน VS Code Source Control ได้ merge commit `9b0d94d`; เคยติด Vim commit editor และออกด้วย `:qa!`; ตรวจ `git status --short --branch` เป็น `## main`; ดู graph ด้วย `git log --oneline --graph --decorate --all -5` เห็น merge commit รวม `9690cff` กับ `e0461f9`; ลบ branch ที่ merge แล้วด้วย `git branch -d docs/conflict-practice`; ตรวจเหลือ `* main` และ `## main`; Knowledge Sync: เพิ่ม `_docs/git/concepts/merge-conflict.md`, อัปเดต `_docs/git/concepts/index.md`, เพิ่ม section `Merge Conflict` ใน `_docs/git/commands.md`, และเพิ่ม Merge Conflict ใน Git Learning sidebar; ตรวจ `_docs` ด้วย `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`
- 2026-07-09 — ✅ Step 0.4.6 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: ฝึก Undo ใน Git ด้วย `ApoRaviz_DevEng`; เริ่มจาก `main` clean; เพิ่มบรรทัดทดลองใน `README.md` แล้วอ่าน `git status --short` เห็น ` M README.md`; ดู diff ด้วย `git diff -- README.md`; ใช้ VS Code Source Control `Discard Changes` เพื่อทิ้ง change เทียบกับ `git restore README.md` แล้วตรวจ status ว่าง; เพิ่มบรรทัดทดลองใหม่แล้ว `git add README.md` เห็น `M  README.md`; ใช้ VS Code `Unstage` เทียบกับ `git restore --staged README.md` เห็นกลับเป็น ` M README.md`; ใช้ `git restore README.md` ทิ้ง change จน status ว่าง; อธิบาย `git commit --amend` ว่าใช้แก้ commit ล่าสุดเมื่อยังไม่ push แต่ไม่ลงมือกับ merge commit ล่าสุดเพื่อไม่ทำ history บทเรียน conflict เพี้ยน; ฝึก `git revert HEAD` กับ commit ทดลองจริงและเห็นว่า revert สร้าง commit ใหม่เพื่อย้อนผล ไม่ลบ commit เดิม; ต่อด้วย branch lab `docs/revert-a-practice` สร้าง commit A/B/C/D แล้วลอง `git revert 5a5e3fc` เพื่อย้อน commit B ที่อยู่กลาง history จนเกิด conflict ใน `revert-a-practice.txt`; อ่าน `UU`, conflict marker, และเข้าใจว่าตอน conflict มนุษย์กำหนด final content เองได้ จึงทดลอง resolve เป็น `A`/`E`/`F`, `git add`, แล้ว `git revert --continue` ได้ commit `78019fa Revert "Add B line"`; สร้าง backup branch `backup/revert-a-practice-before-reset` ก่อน `git reset --hard 5a5e3fc`; เห็นว่า `reset --hard` ย้าย branch ปัจจุบันกลับไป commit B จริงและไฟล์กลับเป็น `A`/`B` ส่วน commit หลัง B หลุดจาก branch นั้นแต่กู้ดูได้เมื่อมี backup branch; ทดลอง VS Code Graph แล้วพบว่า Source Control/Git Graph ที่ใช้อยู่ไม่มีเมนู reset branch ตรง ๆ จึงใช้ `Copy Commit Hash` แล้ว reset ผ่าน terminal; เข้าใจ mapping กับ VS Code: Discard Changes, Unstage, Copy Commit Hash, และข้อจำกัดของปุ่ม Undo Last Commit; Knowledge Sync: เพิ่ม `_docs/git/concepts/undo-in-git.md`, อัปเดต `_docs/git/concepts/index.md`, เพิ่ม section `Undo` ใน `_docs/git/commands.md`, เพิ่ม Undo In Git ใน Git Learning sidebar, และเติม lab note เรื่อง revert old hash conflict / backup branch / reset ผ่าน copied hash; Validation: `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่าน, `ApoRaviz_DevEng` clean on `main`; หลัง review ลบ practice branch `docs/revert-a-practice` และ `backup/revert-a-practice-before-reset` เหลือ `* main` (main history noise เก็บเป็น decision ถัดไป ไม่ block 0.4.6)
- 2026-07-09 — ✅ Step 0.4.7 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: ฝึก Git safety tools ด้วย `ApoRaviz_DevEng`; เริ่มจาก `main` clean; ใช้ `git reflog --oneline -10` อ่านร่องรอยจริงจาก lab 0.4.6 เช่น `HEAD@{1}: reset`, `HEAD@{4}: commit: Revert "Add B line"`, และเข้าใจว่า `git log` คือประวัติ branch ส่วน `git reflog` คือประวัติการขยับ `HEAD`/branch pointer ในเครื่องนี้; สร้าง branch กู้คืนด้วย `git branch recovered/from-reflog 78019fa`, switch ไปดูไฟล์ `revert-a-practice.txt` เห็น `A`/`E`/`F`, แล้วกลับ `main` และลบ branch กู้คืน; ฝึก `stash` โดยเพิ่มบรรทัดทดลองใน `README.md`, ใช้ `git stash push -m "Practice stash README line"`, ดู `git stash list`, ดู diff ด้วย `git stash show -p stash@{0}`, ใช้ `git stash apply stash@{0}` แล้วเห็นว่า stash ยังอยู่, restore working tree กลับสะอาด, จากนั้นใช้ `git stash pop stash@{0}` แล้วเห็นว่า change กลับมาและ stash ถูก drop; ฝึก `cherry-pick` โดยสร้าง branch `docs/cherry-source-practice` เพิ่มไฟล์ `cherry-pick-practice.txt` ได้ commit `2666935`, สร้าง branch ปลายทาง `docs/cherry-target-practice` จาก `main`, ยืนยันว่าไฟล์ยังไม่มี, ใช้ `git cherry-pick 2666935` แล้วได้ commit ใหม่ `2b05d8e` พร้อมไฟล์เดียวกัน และเข้าใจว่า cherry-pick คือ copy patch มาเขียนเป็น commit ใหม่ hash จึงเปลี่ยน; เข้าใจว่าถ้าอีก branch มีหลาย commit สามารถ cherry-pick เฉพาะบาง commit ได้ แต่ต้องระวัง dependency ระหว่าง commit; cleanup branch lab แล้วเหลือ `* main`; Knowledge Sync: เพิ่ม `_docs/git/concepts/reflog.md`, `_docs/git/concepts/stash.md`, `_docs/git/concepts/cherry-pick.md`, อัปเดต `_docs/git/concepts/index.md`, เพิ่ม section Reflog/Stash/Cherry-pick ใน `_docs/git/commands.md`, และเพิ่ม sidebar Git Learning สำหรับสาม concept นี้; Validation: `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่าน, `ApoRaviz_DevEng` clean on `main`, stash ว่าง, branch เหลือ `* main`
- 2026-07-13 — ✅ Step 0.4.8 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: เรียน Commit Message Convention ว่า commit message คือหัวข้อของ commit history และรูปแบบเบื้องต้นคือ `type(scope): summary`; แยก `type` ว่าเป็นประเภทงาน เช่น `docs`, `feat`, `fix`, `chore`, `test`, `refactor`, `scope` ว่าเป็นพื้นที่ที่แก้ เช่น `readme`, `git`, `login-form`, `gitignore`, และ `summary` ว่าเป็นคำอธิบายสั้น ๆ ของสิ่งที่ทำ; ฝึกแปลง message เช่น `Add test reminder to README` เป็น `docs(readme): add test reminder`, `.gitignore` ignore `dist/` เป็น `chore(gitignore): ignore dist output`, เพิ่ม login form เป็น `feat(login-form): add login form component`, และ bug login redirect เป็น `fix(login): redirect after successful login`; ฝึก commit จริงใน `ApoRaviz_DevEng` โดยเพิ่มบรรทัด `Git practice uses clear commit messages.` ใน `README.md`, ตรวจ diff, stage, แล้ว commit ด้วย `docs(git): add commit message practice note` ได้ commit `bb0f479`; หลัง commit ตรวจ `git status --short --branch` กลับมา `## main`; Knowledge Sync: เพิ่ม `_docs/git/concepts/commit-message-convention.md`, อัปเดต `_docs/git/concepts/index.md`, เพิ่มตัวอย่าง convention ใน `_docs/git/commands.md`, และเพิ่ม sidebar Git Learning สำหรับ Commit Message Convention; Validation: `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่าน, `ApoRaviz_DevEng` clean on `main`, branch เหลือ `* main`; Reviewer เพิ่ม note แยก bare `fix`/`test` (ควรเลี่ยง) ออกจาก `fix`/`test` ที่ใช้เป็น type prefix ใน `_docs/git/concepts/commit-message-convention.md`
- 2026-07-13 — ✅ Step 0.4.9 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: เชื่อม local repo `ApoRaviz_DevEng` กับ GitHub remote `https://github.com/ApoRaviz/ApoRaviz_DevEng.git`; เริ่มจาก `git status --short --branch` เป็น `## main` และ `git remote -v` ว่าง; ใช้ `git ls-remote` และ `git ls-remote --heads` ตรวจพบว่า remote ไม่ว่าง มี `515474f refs/heads/main`; เพิ่ม remote ด้วย `git remote add origin ...`, `git fetch origin`, แล้วอ่าน `origin/main` เป็น remote-tracking branch หลัง fetch; อธิบายละเอียดว่า `main` คือ local branch, `origin` คือชื่อ remote URL, `origin/main` คือ snapshot ล่าสุดของ GitHub main หลัง fetch ไม่ใช่ live view, และ `git ls-remote` คือถาม remote โดยตรง; ตรวจ `origin/main` พบ initial commit มี `.gitignore` และ `LICENSE`; ตัดสินใจ merge remote initial commit เพื่อเก็บ MIT `LICENSE` ด้วย `git merge origin/main --allow-unrelated-histories`; เกิด `CONFLICT (add/add)` ใน `.gitignore`, อ่าน `AA .gitignore` และ `A LICENSE`; resolve โดยใช้ local Angular `.gitignore` เป็นหลัก, เพิ่ม rule ที่เหมาะจาก remote เช่น `/e2e/test-output/`, `/.env`, `*.tsbuildinfo`, แต่ไม่ ignore `package-lock.json`; ตรวจว่าไม่มี conflict marker และไม่มี `package-lock` rule แล้ว `git add .gitignore`; commit merge ด้วย `chore(git): merge GitHub initial files` ได้ merge commit `49f8c25`; push สำเร็จด้วย `git push -u origin main`, เห็น `515474f..49f8c25 main -> main` และตั้ง upstream; ตรวจ `git status --short --branch` เป็น `## main...origin/main` และ `git log --oneline --decorate origin/main -3` เห็น `49f8c25 (HEAD -> main, origin/main, origin/HEAD)`; Knowledge Sync: เพิ่ม `_docs/git/concepts/remote.md`, อัปเดต `_docs/git/concepts/index.md`, เพิ่ม Remote command flow ใน `_docs/git/commands.md`, และเพิ่ม Remote ใน Git Learning sidebar; Validation: `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่าน, `ApoRaviz_DevEng` clean และ sync `## main...origin/main` (`origin/main` = `49f8c25`); Reviewer เอา `remote` ออกจาก Terms To Add Later ใน `_docs/git/concepts/index.md` (ย้ายเข้า Current Concepts แล้ว) — ปิด decision main history noise ว่าเก็บ history ตามจริง (push ขึ้น GitHub แล้ว ไม่ rewrite)
- 2026-07-13 — ✅ Step 0.4.10 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: ฝึก Git daily workflow ด้วย remote จริงใน `ApoRaviz_DevEng`; เริ่มจาก `git status --short --branch` เห็น `## main...origin/main`; ใช้ `git fetch origin` เพื่ออัปเดตภาพ `origin/main`; อ่าน graph ด้วย `git log --oneline --decorate --graph --all -8` เห็น `HEAD -> main`, `origin/main`, และ `origin/HEAD` อยู่ที่ `49f8c25`; แก้ `README.md` เพิ่มบรรทัด `Daily Git workflow checks local and remote sync before pushing.`, ตรวจ diff, stage และ commit ด้วย `docs(git): add daily workflow sync note` ได้ commit `0dfe8eb`; อ่าน `git status --short --branch` เห็น `## main...origin/main [ahead 1]` และเข้าใจว่า local มี commit ใหม่ 1 อันที่ยังไม่ได้ push; อ่าน log เห็น `HEAD -> main` อยู่ที่ `0dfe8eb` ส่วน `origin/main` ยังอยู่ `49f8c25`; ใช้ `git push` ได้โดยไม่ต้องระบุ `origin main` เพราะตั้ง upstream แล้วใน 0.4.9; หลัง push ตรวจ `git status --short --branch` กลับเป็น `## main...origin/main` และ `git log --oneline --decorate -3` เห็น `0dfe8eb (HEAD -> main, origin/main, origin/HEAD)`; Knowledge Check ผ่าน: `[ahead 1]` คือ local มี commit ที่ยังไม่ push, `git push` เฉย ๆ ใช้ได้เพราะรู้จัก upstream แล้ว, และควร `git fetch origin` ก่อนอ่าน `origin/main` เพื่อดูว่าหลัง fetch มีอะไรไม่ตรงกันไหม; Knowledge Sync: เพิ่ม `Daily Pull/Push Workflow` ใน `_docs/git/commands.md` และ link กลับไป concept `Remote`; Validation: `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่าน, `ApoRaviz_DevEng` clean และ sync `## main...origin/main` (local main = `origin/main` = `0dfe8eb`); Claude review verify: Daily Pull/Push Workflow section ถูกต้อง (fetch/`[ahead 1]`/`git push`/upstream/`pull --ff-only`), commit `0dfe8eb` และ decorate `HEAD -> main, origin/main, origin/HEAD` ตรง repo จริง
- 2026-07-14 — ✅ Step 0.5.1 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: เรียน Environment Variable ว่าเป็นค่า key/value ที่ shell, OS, dev tool, server หรือ CI/CD ส่งให้ process ตอนเริ่มทำงาน; ใช้ `git status --short --branch` ตรวจ `ApoRaviz_DevEng` เป็น `## main...origin/main`; ใช้ `printenv HOME` เห็น `/Users/aporaviz` เพื่อพิสูจน์ env ที่มีอยู่แล้วและไม่ใช่ secret; ใช้ `node -p "process.env.HOME"` เห็น Node.js อ่าน env ผ่าน `process.env` ได้; อธิบายว่าใช้ `HOME` เพราะปลอดภัยและไม่ต้องเอา `API_KEY` จริงมาเล่น; ใช้ `printenv PATH` เพื่อเห็นว่า `PATH` เป็น env ที่ shell ใช้หา command; ใช้ `which node` พบ shell ชี้ Node `v22.14.0`; ใช้ `nvm use 24` แล้ว `which node` เปลี่ยนเป็น Node `v24.16.0` และ `node -v` เป็น `v24.16.0`, จึงเห็นจริงว่า `nvm use` เปลี่ยน environment ของ shell รอบนั้นโดยเฉพาะ `PATH`; ตอบ Knowledge Check ว่า env สำหรับ `DATABASE_URL` ช่วยเรื่องความปลอดภัย เพราะไม่ hardcode secret ลง code; อธิบายว่า secret เช่น password/API key ถ้า commit แล้วจะติด Git history ต่อให้ลบบรรทัดใน commit ถัดไปก็ยังเสี่ยง และถ้าหลุดจริงควร rotate/revoke secret; Knowledge Sync: เพิ่ม `_docs/nodejs/concepts/environment-variable.md`, `_docs/nodejs/concepts/secret.md`, `_docs/nodejs/concepts/index.md`, เพิ่ม `Environment Variables` ใน `_docs/nodejs/commands.md`, update Node.js hub และ sidebar; Validation: `source ~/.nvm/nvm.sh && nvm use 24 && npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่าน, `ApoRaviz_DevEng` clean และ sync `## main...origin/main`; Claude review verify: env/HOME/PATH/process.env/nvm use ถูกต้อง, secret rotate/revoke priority ถูก, แยก Angular env files (public frontend) vs secret (backend) ชัด — ข้อเสนอไม่บล็อก: เปลี่ยน `/Users/aporaviz` ในตัวอย่าง public docs เป็น placeholder ตาม machine-agnostic
- 2026-07-14 — ✅ Step 0.5.2 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: เรียน `.env` ว่าเป็นไฟล์ local สำหรับเก็บ env ตอนพัฒนา และต้องใช้คู่กับ `.gitignore`; เริ่มจากผู้เรียนเผลออยู่ `ApoRaviz_Portfolio` แล้วกลับเข้า `ApoRaviz_DevEng` ก่อนทำงานจริง; ตรวจ `git status --short --branch` เป็น `## main...origin/main`; ใช้ `grep -n '^\\.env' .gitignore` แล้วไม่มี output จึงยังไม่สร้าง `.env`; อ่านท้าย `.gitignore` ด้วย `tail -30 .gitignore`; เพิ่ม rule `# Environment files`, `.env`, `.env.*`, `!.env.example`; ตรวจ diff และแก้ newline ท้ายไฟล์; สร้าง `.env.example` มี `DATABASE_URL=` และ `JWT_SECRET=` แบบไม่มีค่าจริง; สร้าง `.env` ด้วยค่าปลอม `DATABASE_URL=postgres://local-dev-placeholder` และ `JWT_SECRET=local-dev-placeholder`; อ่าน `git status --short` เห็นเฉพาะ `M .gitignore` และ `?? .env.example` ไม่เห็น `.env`; ใช้ `ls -la .env .env.example` ยืนยันว่า `.env` มีอยู่จริง; ใช้ `git check-ignore -v .env` ได้ `.gitignore:60:.env .env`; ใช้ `git check-ignore -v .env.example` ได้ exception rule `!.env.example`; ตรวจ `cat .env.example` และแก้ newline ท้ายไฟล์; stage เฉพาะ `.gitignore` และ `.env.example`, ตรวจ `git diff --cached`, commit ด้วย `chore(env): add local env template` ได้ commit `7edc0d6`, เห็น `[ahead 1]`, ตรวจ `.env` ยังถูก ignore แล้ว `git push` สำเร็จ `0dfe8eb..7edc0d6 main -> main`; หลัง push `git status --short --branch` กลับเป็น `## main...origin/main` และ `git log --oneline --decorate -3` เห็น `7edc0d6 (HEAD -> main, origin/main, origin/HEAD)`; Knowledge Check ผ่าน: `.env.example` commit ได้เพราะเป็นค่าว่าง/template ส่วน `.env` มีค่าจริง local จึงห้าม commit; อธิบาย dev หลายเครื่องว่าให้แชร์ชื่อ key ผ่าน `.env.example` แล้วเติมค่าจริงผ่าน password manager, hosting dashboard, CI/CD secrets หรือ secret manager ไม่ส่ง `.env` ผ่าน Git; Knowledge Sync: เพิ่ม `_docs/nodejs/concepts/dotenv-file.md`, อัปเดต Node concepts/index/sidebar, เพิ่ม `Local .env Files` ใน `_docs/nodejs/commands.md`, และเพิ่มตัวอย่าง `.env`/`.env.example` ใน `_docs/git/concepts/gitignore.md`; Validation (ตามที่รายงาน): docs build ผ่าน Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่าน, `ApoRaviz_DevEng` clean และ sync `## main...origin/main` (`origin/main` = `7edc0d6`); Claude review verify เนื้อหา: `.env`/`.env.example`/`!.env.example` exception, `git check-ignore -v` (ทั้ง `.env` และ `.env.example`), และ dev หลายเครื่องใช้ `.env.example` + password/secret manager/hosting dashboard — ถูกต้อง (รอบนี้ยึด verify จาก learner log + เนื้อหา docs ไม่ได้ re-run bash ตาม preference read-only)
- 2026-07-14 — ✅ Milestone: Phase 0 (Foundation) ครบทุก step และผ่าน Claude review ตามกติกา 2.4 แล้ว: 0.1 Project Initialization & Tooling (0.1.1–0.1.4), 0.2 Configuration Deep-dive (0.2.1–0.2.4), 0.3 Styling Foundation (0.3.1–0.3.2), 0.4 Git Workflow (0.4.1–0.4.10), 0.5 Secret Management เบื้องต้น (0.5.1–0.5.2) รวม 22 step; หลังจากนี้เข้า Phase 1 เริ่มที่ 1.0.1 API Contract
- 2026-07-15 — ✅ Step 1.0.1 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว (ผ่านรอบ 2 หลังแก้ security): ออกแบบ API contract ของ DevEng MVP ก่อนเขียน backend จริง ที่ `ApoRaviz_DevEng/docs/api-contract.md` ครอบคลุม 6 endpoint — `GET /health`, `POST /auth/register`, `POST /auth/login`, `POST /invite-tokens` (admin only), `POST /translations`, `GET /translations`; กำหนด design principle ว่า MVP เป็น English -> Thai เท่านั้น, public API ยังไม่ expose `targetLanguage`, response ส่งเฉพาะที่ client ต้องใช้, และ server-owned fields (`id`, `role`, `createdAt`, `isActive`, `tokenHash`) ไม่รับจาก request body; Claude review รอบแรกพบ blocking issue 4 ข้อ: (1) invite token error แยก 400 (invalid/expired) กับ 409 (used) ทำให้ enumerate ได้ว่า token string ไหนเคยมีอยู่จริง ขัดกับหลัก "ไม่ leak sensitive state" ที่ contract เขียนไว้เอง, (2) register 409 email ซ้ำ = user enumeration ที่ยังไม่มี decision รองรับและไม่ได้ระบุลำดับตรวจ, (3) contract ขาด `Authorization: Bearer` header rule, token lifetime, `inputText` max length, และ register validation constraints, (4) `_docs` มีคำว่า "ถูกทรง" ที่ไม่สื่อความหมาย, ตรรกะ privilege escalation ขาดท่อนผลลัพธ์, และ error shape ของ 401 ไม่ตรงกับ 400; Codex แก้ครบทั้ง 4 ข้อ: invite token ที่ใช้ไม่ได้ทุกกรณี (ไม่มีจริง/หมดอายุ/ถูกใช้แล้ว/ถูก revoke) รวมเป็น `400 Bad Request` message กลาง `Invite token is invalid or cannot be used`, `409 Conflict` เหลือเฉพาะ email ซ้ำและเกิดหลัง invite token ผ่านแล้วเท่านั้น พร้อม decision ว่า backend ต้องตรวจ invite token ก่อนตรวจ email ซ้ำ (ปิด enumeration หลัง invite gate) และเขียน trade-off ของ 409 ไว้ชัด, เพิ่ม Shared Request Rules สำหรับ `Authorization: Bearer <accessToken>` + Auth Response shape (`tokenType: Bearer`, `expiresIn: 3600`) พร้อม decision ว่า MVP access token อายุ 1 ชั่วโมงและยังไม่มี refresh token, เพิ่ม validation table (`displayName` 1-80, `password` 8-128, `inputText` 1-1000, email normalized ก่อนเช็ก uniqueness) พร้อมเหตุผลว่า `inputText` max length กัน AI provider cost/rate limit ใน Phase 2; Knowledge Sync: เพิ่ม `_docs/backend/concepts/api-contract.md` (ภาพจำร้านอาหาร -> ภาษาคนธรรมดา -> technical term -> flow -> status code 200/201/400/401/403/404/409/500 -> server-owned field -> error ควรบอกแค่ไหน), เพิ่ม `_docs/backend/concepts/privilege-escalation.md` เป็น concept page reusable, อัปเดต `_docs/backend/concepts/index.md` และ sidebar ใน `_docs/.vitepress/config.mts`; Validation (ตามที่รายงาน): `npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่านทั้ง `_docs` และ `ApoRaviz_DevEng`; Claude review verify รอบ 2: blocking ทั้ง 4 ข้อปิดครบ, invite token ไม่เหลือ status/message oracle, ลำดับ token-before-email เขียนเป็น decision ที่เขียน test ได้จริง, status code 400/401/403/409 ใช้ถูกตาม semantic, `_docs` ทั้ง 2 หน้า Thai-first ตาม TEACHING_RULES ไม่มี business rule เฉพาะ DevEng และไม่มี personal context — ข้อเสนอไม่บล็อก (ยกไป 1.0.2): นิยาม email normalization ให้ตรงกับ unique index ตอนออกแบบ schema, และ `POST /invite-tokens` ยังไม่คืน `id` ซึ่งจะต้องใช้ตอนทำ revoke ตาม field `revoked_at` ที่ lock ไว้ใน `AGENTS.md`
- 2026-07-17 — ✅ Step 1.0.2 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: ออกแบบ database schema ของ DevEng MVP ก่อนเขียน migration จริง ที่ `ApoRaviz_DevEng/docs/database-schema.md` ครอบคลุม 3 table ตาม entity ใน `Vision_Decisions.md` ข้อ 8.7 — `users`, `invite_tokens`, `translations`; design principle: `uuid` เป็น primary key, `timestamptz` สำหรับเวลาทั้งหมด, `created_at` เป็น database-owned default `now()`, FK ไป `users.id` ใช้ `ON DELETE RESTRICT / NO ACTION` เพื่อไม่ให้ history/audit หายเงียบ, ไม่ใส่ `is_active` เพราะยังไม่มี disable user flow, และ validation สำคัญทำ 2 ชั้น (API validation + DB constraint); `users` มี `email` (trim) แยกจาก `email_normalized` (trim + lowercase) โดยตั้ง unique ที่ `email_normalized`, `role` เป็น text + check `IN ('admin','user')` แทนการแตก RBAC table ตั้งแต่ MVP, `display_name` check 1-80 chars ตรงกับ API contract, และใช้ `password_hash` ตั้งแต่ design เพื่อไม่เปิดทางเก็บ password ดิบ (password hashing จะเรียนจริงใน 1.6.3); `invite_tokens` มี `id` เป็น PK เพื่อให้ future revoke endpoint อ้าง record ได้โดยไม่ใช้ token ดิบ/`token_hash` (ปิด carry-over จาก 1.0.1), เก็บ `token_hash` unique ไม่เก็บ token ดิบ, ครบ field ที่ lock ไว้ใน `AGENTS.md` (`token_hash`, `expires_at`, `used_at`, `used_by_user_id`, `created_by_admin_id`, `revoked_at`) และเขียน token status rule (`expires_at > now()`, `used_at IS NULL`, `revoked_at IS NULL`) ที่ผูกกลับไป single 400 path ใน `api-contract.md` เพื่อไม่เปิด token enumeration; `translations` มี FK `user_id`, check `char_length(input_text) BETWEEN 1 AND 1000` ซ้ำชั้นกับ API validation, และ composite index `(user_id, created_at DESC)` ที่ match query history จริงของ `GET /translations`; Knowledge Sync: เพิ่ม `_docs/postgresql/concepts/` 5 หน้า — `primary-key.md`, `foreign-key.md`, `unique-constraint.md`, `database-index.md`, `check-constraint.md` พร้อม `concepts/index.md`, อัปเดต `_docs/postgresql/index.md` (เพิ่ม section Concepts + ศัพท์ unique/check constraint + กติกาออกแบบ schema) และ sidebar ใน `_docs/.vitepress/config.mts`; Validation (ตามที่รายงาน): `npm run docs:build` ผ่านด้วย Node `v24.16.0` / npm `11.13.0`, `git diff --check` ผ่านทั้ง `_docs` และ `ApoRaviz_DevEng`; Claude review verify: schema align กับ `api-contract.md` ทุก field (response ของ `GET /translations` ครบใน `translations`, `display_name` 1-80 ตรงกัน, `input_text` 1-1000 ตรงกัน, ไม่มี column เกินที่ contract ไม่ได้ใช้), carry-over จาก 1.0.1 ปิดครบทั้ง 2 ข้อ (email normalization นิยามชัดพอสำหรับ unique index, `invite_tokens.id` มีแล้ว), PK/FK/unique/index/check เหมาะสมกับ MVP, `RESTRICT` ถูกต้องเพราะ MVP ยังไม่มี delete-user flow และควร fail ดังกว่าลบ audit เงียบ ๆ, `_docs` ทั้ง 5 หน้า Thai-first ตาม TEACHING_RULES (ภาพจำ -> ภาษาคนธรรมดา -> technical term -> flow -> จุดที่มักงง -> เช็กตัวเอง) reusable ไม่มี business rule เฉพาะ DevEng และไม่มี personal context — ข้อเสนอไม่บล็อก: (1) `expires_at` rule "must be future when created" บังคับด้วย DB check constraint ไม่ได้ เพราะ PostgreSQL ต้องการ IMMUTABLE function ใน `CHECK` แต่ `now()` ไม่ใช่ จึงเป็น API validation เท่านั้น ควรเขียนกำกับไว้กัน implement ผิด, (2) first admin bootstrap ยังไม่มีที่ไหนระบุ — register set `role = 'user'` เสมอ และ `invite_tokens.created_by_admin_id` NOT NULL แปลว่า admin คนแรกต้องมาจาก seed/migration ไม่ใช่ register flow ควรเพิ่มเป็น Open Note, (3) `used_at`/`used_by_user_id` consistency ควรใส่ check `(used_at IS NULL) = (used_by_user_id IS NULL)` ได้เลยตั้งแต่ design เพราะกติกานิ่งแล้ว ไม่ต้องรอ implementation, (4) ยกไป 1.0.3: การันตี "1 token ใช้ได้ 1 คน" ที่ lock ไว้ใน `AGENTS.md` ต้องใช้ atomic conditional update (`UPDATE ... WHERE used_at IS NULL RETURNING`) ใน sequence diagram — schema/check constraint อย่างเดียวกัน race condition ของ concurrent register ด้วย token เดียวกันไม่ได้
- 2026-07-20 — ✅ Step 1.0.3 ผ่าน Learning Loop และ Claude review ตามกติกา 2.4 แล้ว: ออกแบบ sequence diagram ของ DevEng MVP ก่อนเขียน backend จริงที่ `ApoRaviz_DevEng/docs/sequence-diagram.md` โดยแยก 3 use case — Register, Login และ Create Translation — พร้อมภาพรวมผู้ใช้ใหม่ (register แล้วได้ access token ทันที) กับผู้ใช้เดิม (login แล้วได้ access token); Register ตรวจ invite token ก่อนเปิดเผย email conflict, ใช้ email pre-check เป็น UX fast-path แต่ให้ `UNIQUE (email_normalized)` เป็นด่านรับประกันจริง, และปิด race condition ของ 1 token/1 user ด้วย transaction + conditional update ที่ set `used_at`/`used_by_user_id` พร้อมกัน + row-level lock + rollback user เมื่อ update ได้ 0 row; Login รวม user ไม่พบ/password ผิดเป็น generic `401` เพื่อกัน user enumeration และออก access token หลัง verify สำเร็จ; Create Translation ตรวจ access token ก่อน validate `inputText` 1–1000 ตัวอักษร, ใช้ `user_id` จาก verified token ไม่รับจาก request body, เรียก Translation Service แบบ stub ใน Phase 1, บันทึก DB ก่อนตอบ `201`; Knowledge Check ผ่านเรื่อง token-before-email, generic auth error, server-owned ownership, fail-fast validation, register auto-login, transaction/rollback และ concurrent token update; Knowledge Sync: เพิ่ม `backend/concepts/sequence-diagram.md`, `backend/concepts/race-condition.md`, `postgresql/concepts/database-transaction.md`, อัปเดต concept indexes/PostgreSQL hub/sidebar และเพิ่ม Interactive Session Rule ใน `TEACHING_RULES.md` เพื่อแยกรูปแบบ Git-command/code/system-design/Knowledge Sync; Validation: `git diff --check` และ trailing-whitespace check ผ่านทั้งสอง repo, `npm run docs:build` ผ่าน; Claude review verify ว่า diagram ตรง API contract/DB schema, concurrency guarantee ถูกต้อง, Workspace Docs Thai-first/reusable/topic-first ไม่มี business rule เฉพาะ DevEng หรือ personal context และ navigation/link ถูกต้อง — นำข้อเสนอ non-blocking ไปปิด open note ใน `database-schema.md` และเขียน email pre-check vs unique guarantee ให้ชัดแล้ว
- 2026-07-23 — ✅ Step 1.1.3 (NestJS Controller และ `GET /health`) ผ่าน Learning Loop และ Independent Review/QA (Claude) ตามกติกา 2.4 แล้ว: สร้าง `HealthController` ที่ `backend/src/health/health.controller.ts` — `@Controller('health')` + `@Get()` คืน `{ status: 'ok' }` (typed `{ status: 'ok' }`) และลงทะเบียนใน `HealthModule.controllers`; ถอด scaffold ตัวอย่างออกครบ (ลบ `app.controller.ts`, `app.service.ts`, `app.controller.spec.ts`, `app.e2e-spec.ts`) และ `AppModule` เหลือแค่ `imports: [HealthModule]` ไม่มี metadata ว่างเกินจำเป็น; ยังไม่มี `HealthService` (ยกไป 1.1.4); เพิ่ม backend E2E `test/health.e2e-spec.ts` (Jest + Supertest) เรียก `GET /health` คาด `200 {"status":"ok"}` + `afterEach` close app, และ `test/tsconfig.json` แยก type-check ให้ครอบ `src/`+`test/` ด้วย jest/node types โดย `noEmit`; ลบ `baseUrl` ออกจาก `backend/tsconfig.json` แทนการเพิ่ม `ignoreDeprecations` (ปลอดภัยเพราะไม่มี path alias/non-relative source import); Roadmap decision: เพิ่ม Step 1.1.8 Backend E2E Test (Jest + Supertest) แยกความหมายจาก 1.1.7 Unit Test และ 1.7.3 Playwright full E2E, ปรับ Phase 1 เป็น ~41 loops, sync 1.1.7 ให้ใช้ Jest (ไม่เหลือ Vitest/Jest) ทั้งใน `Roadmap_Progress.md` และ `Project_Vision_Discussion_Summary_V0_0_12.md`; Independent verify (Claude run เอง): lint 0/0, build ผ่าน, unit 1/1, e2e 1/1, `npx tsc --noEmit -p test/tsconfig.json` exit 0, runtime จริง `GET /health`=200 `{"status":"ok"}` / `POST /health`=404 / `GET /`=404 (ยืนยัน AppController ถูกถอด), `docs:build` ผ่านไม่มี dead link, `git diff --check` ผ่านทั้งสอง repo; Knowledge Sync: เพิ่ม `nestjs/concepts/controller.md` (route=method+path, path composition, module registration, JSON serialization, return type vs status, thin controller) + อัปเดต `nestjs/commands.md` (generate controller + `nest --version`), `nestjs/nest-cli-project-structure.md` (jest-e2e.json vs test/tsconfig.json, TS version ข้าม nested project, baseUrl vs ignoreDeprecations), `nodejs/teach/06` (version map Node/NVM/npm/TS/Angular CLI/Nest CLI), index/sidebar — Thai-first/topic-first ใช้ตัวอย่าง generic ไม่ผูก DevEng business rule; ข้อเสนอไม่บล็อก: (1) `health.controller.spec.ts` ยัง assert แค่ `toBeDefined()` (ตาม scaffold) การ assert `getHealth()` คืน `{status:'ok'}` จะแน่นขึ้น — เหมาะไปเสริมตอน 1.1.7 Unit Test, (2) e2e test file ถูก maintain ตั้งแต่ตอนนี้ก่อนบทเรียน 1.1.8 อย่างเป็นทางการ — โอเคเพราะ scaffold มี e2e infra อยู่แล้ว Codex แค่รักษาให้เขียว 1.1.8 จะ formalize ความเข้าใจของสิ่งที่มีใน repo แล้ว
- 2026-07-23 — ✅ Step 1.1.2 (NestJS Module) ผ่าน Learning Loop และ Independent Review/QA (Claude) ตามกติกา 2.4 แล้ว: สร้าง `HealthModule` เปล่า (`@Module({})`) ที่ `backend/src/health/health.module.ts` ด้วย `npx nest generate module health` (ตรวจ `--dry-run` ก่อน) และ CLI เพิ่ม `HealthModule` เข้า `AppModule.imports` ให้เอง; ยึด scope 1.1.2 เคร่งครัด — ยังไม่มี HealthController/HealthService หรือ `/health` route (ยกไป 1.1.3–1.1.4); เรียนแยก TypeScript `import`/`export class` ออกจาก Nest `imports`/`exports` metadata, provider registration + constructor injection, provider visibility ข้าม module (ต้อง export ที่เจ้าของ + import module ที่ผู้ใช้), และ custom repository (provider) เทียบ ORM repository (`TypeOrmModule.forFeature()` — ยังไม่ implement); Independent verify (Claude run เอง): backend `npm run lint` 0/0, `npm run build` ผ่าน, unit 1/1, e2e 1/1, runtime log `HealthModule dependencies initialized` + map เฉพาะ `{/, GET}` (ยืนยันไม่มี endpoint ใหม่), `docs:build` ผ่านไม่มี dead link, `git diff --check` ผ่านทั้งสอง repo; Knowledge Sync: เพิ่ม `nestjs/concepts/module.md` (4 ช่อง metadata, DI, cross-module, custom vs ORM repo), `nestjs/commands.md` (command reference เติมต่อได้: new + generate module + dry-run/working-dir), `nestjs/concepts/index.md`, อัปเดต `nestjs/index.md`, `nestjs/nest-cli-project-structure.md`, sidebar — Thai-first/topic-first ใช้ตัวอย่าง generic (Users/Reports) ไม่ผูก DevEng business rule; ข้อเสนอไม่บล็อก: docs commit `5dd5c06` ผูก `claude/extensions.md` + `claude/index.md` (นอก scope 1.1.2) มาด้วย — เนื้อหา reusable/ไม่มี personal context จึงไม่บล็อก แต่ควรแยก commit ให้ scope ชัดในรอบถัดไป
- 2026-07-22 — ✅ Step 1.1.1 (Nest CLI และโครงสร้าง project) ผ่าน Learning Loop และ Independent Review/QA (Claude) ตามกติกา 2.4 แล้ว: สร้าง NestJS 11 backend แบบ standard mode ใน `ApoRaviz_DevEng/backend/` ด้วย Nest CLI (npm, `--strict`, `--skip-git`) — Angular อยู่ที่ root, backend อยู่ในโฟลเดอร์ย่อย, Git repo เดียวกันแต่แยก `package.json`/`package-lock.json` และไม่มี nested `backend/.git`; scaffold เป็นค่าเริ่มต้น (default AppModule/AppController/AppService, unit + e2e test) ยังไม่เพิ่ม business Module/Controller/Service, DB หรือ API ของ DevEng และยังไม่ใช้ Nx/Nest monorepo mode ตาม scope; แก้ `src/main.ts` เป็น `void bootstrap();` แก้ ESLint `no-floating-promises`; เพิ่ม `.gitignore` สำหรับ `/backend/node_modules`, `/backend/dist`, `/backend/coverage`, เพิ่ม `.nvmrc` = `24` ตาม baseline, อัปเดต root `README.md` อธิบายโครงสร้าง frontend/backend; Knowledge Sync: เพิ่ม `nestjs/nest-cli-project-structure.md` (Nest CLI vs NestJS, file map, main.ts/nest-cli.json, start/build flow, source-of-truth vs generated) และ `backend/concepts/monorepo.md` (monorepo/managed monorepo/Nx/Nest monorepo mode/polyrepo), อัปเดต `nestjs/index.md`, `backend/concepts/index.md`, sidebar; Independent verify (Claude re-run เอง): backend `npm run lint` 0/0, `npm run build` ผ่าน, unit test 1/1, e2e 1/1, `git check-ignore` ยืนยัน node_modules/dist/coverage ถูก ignore, `git status` เห็นเฉพาะ 16 ไฟล์งานจริง ไม่มี generated leak, `docs:build` ผ่านไม่มี dead link, `git diff --check` ผ่านทั้งสอง repo — Workspace Docs Thai-first/reusable/topic-first (ใช้ตัวอย่าง `api/` generic ไม่ผูก DevEng) ไม่มี business rule เฉพาะหรือ personal context; ข้อเสนอไม่บล็อก: (1) `backend/README.md` ยังเป็น boilerplate เริ่มต้นของ Nest (มีลิงก์ Mau/Discord/AWS) พิจารณาตัดให้เหลือเฉพาะที่ใช้จริงหรือลบเพราะ root README ครอบคลุมแล้ว, (2) `nest-cli-project-structure.md` เป็น lesson วางตรงใต้ `nestjs/` ได้เพราะโฟลเดอร์ยังแบน แต่ถ้าอนาคตมีบทเรียน NestJS เพิ่มควรพิจารณาโครง `nestjs/teach/`, (3) `void bootstrap()` แค่บอก linter ว่าตั้งใจไม่ใช้ค่า Promise ไม่ได้จัดการ rejection จริง — docs อธิบายถูกแล้วว่าไม่ได้เพิ่ม error handling ให้คงความชัดนี้ไว้เมื่อสอน bootstrap ลึกขึ้น
- 2026-07-21 — ✅ Step 1.0.4 ผ่าน Learning Loop และ Independent Review/QA (Claude) ตามกติกา 2.4 แล้ว: ออกแบบ low-fidelity wireframe ของ DevEng MVP ก่อนเขียน component จริงที่ `ApoRaviz_DevEng/docs/wireframe.md`; Screen Map มี Login, Register (invite-only) และ Translation Workspace เท่านั้น ไม่มี Admin UI; Auth Shell ใช้ Login/Register form เดียวกันผ่าน Sliding Auth Panel (visual panel เลื่อนสลับกับ form, 300–500ms, ป้องกันกดสลับซ้ำระหว่าง animation, keyboard focus ย้ายไป form ใหม่, mobile/`prefers-reduced-motion` มี fallback ไม่พึ่ง sliding, ไม่เพิ่ม Apple/Google login เพราะยังไม่มี OAuth endpoint); Register form มีเฉพาะ `inviteToken`/`displayName`/`email`/`password` ตรง `api-contract.md` ไม่มี server-owned field, สมัครสำเร็จได้ access token ทันทีไม่เรียก login ซ้ำ; Login ใช้ generic `Invalid email or password`; Translation Workspace รวม Form/Result/History หน้าเดียว, desktop สองคอลัมน์ mobile หนึ่งคอลัมน์, มี state ครบ initial/typing-invalid/loading/success/token-expired/history-empty, History แสดงล่าสุด 20 รายการเรียงใหม่สุดก่อนตาม default ของ `GET /translations` โดยไม่เรียก API เพิ่มเพราะ response มีรายละเอียดครบอยู่แล้ว และไม่เขียนทับ textarea draft; เพิ่ม Step 1.6.9 (Frontend Auth Forms & State) และ 1.6.10 (Sliding Auth Panel & Accessible Motion) ต่อจาก 1.6.8 ก่อน 1.7 Stub AI Endpoint และ sync จำนวน Learning Loop ของ Phase 1 จาก ~38 เป็น ~40 ทั้งใน `Roadmap_Progress.md` และ `Project_Vision_Discussion_Summary_V0_0_12.md`; Logout mechanism ถูกบันทึกเป็น decision gate ที่ต้องรอ JWT storage decision จาก Step 1.6.2 (`localStorage` เคลียร์ฝั่ง frontend ได้เลย แต่ `httpOnly cookie` ต้องเพิ่ม logout endpoint และแก้ `api-contract.md` ก่อน) แทนการ lock กลไกไว้ก่อนตัดสินใจ; Knowledge Sync: เพิ่ม `angular/concepts/wireframe.md`, อัปเดต `angular/concepts/index.md` และ sidebar ใน `.vitepress/config.mts`; Validation: `git diff --check` และ trailing-whitespace check ผ่านทั้งสอง repo (มีแค่ CRLF/LF warning ปกติของ Windows), `npm run docs:build` ผ่านไม่มี dead link; Claude Independent Review verify: wireframe ตรง API contract/DB schema/sequence diagram ทุกจุด ไม่มี control ที่ไม่มี flow รองรับ (Logout ระบุ decision gate ชัดเจนแทนการ lock ก่อนเวลา), Sliding Auth Panel เป็น learning-scope ที่ user (Product Owner) อนุมัติเองและไม่กระทบ API/DB/security ตาม Scope Discipline (ข้อ 2.7), Roadmap 1.6.9–1.6.10 วางตำแหน่งถูกต้องตาม dependency (หลัง JWT storage decision 1.6.2, ก่อน 1.7 ที่ต้องใช้ auth UI จริง), Workspace Docs Thai-first/reusable/topic-first ไม่มี business rule เฉพาะ DevEng หรือ personal context, index/sidebar/link ถูกต้อง — ข้อเสนอไม่บล็อกสำหรับรอบถัดไป: (1) เพิ่มบรรทัดสั้น ๆ ใน `wireframe.md`/1.7.2 ว่า transition กลับจาก "ดู history item" ไปเป็น "พิมพ์ draft ใหม่" ต้องเคลียร์ label `จากประวัติ` และผลลัพธ์เก่าด้วย ไม่ใช่แค่ห้ามเขียนทับ textarea, (2) พิจารณาเพิ่ม generic "โหลด/ส่งไม่สำเร็จ" state แยกจาก validation/token-expired เผื่อ Phase 2 ที่ AI provider ภายนอกอาจ timeout/fail จริง, (3) session-expiry handling (401 -> กลับ Login) ควรระบุว่าขึ้นกับ JWT storage decision จาก 1.6.2 เหมือนที่ระบุไว้กับ Logout เพื่อไม่ให้ signal ไม่ครบ, (4) พิจารณาเพิ่ม sub-label แยก 1.6.1–1.6.8 (backend security mechanics) ออกจาก 1.6.9–1.6.10 (frontend auth UI) ภายใต้หัวข้อ "1.6 Auth/Security Deep-dive" ให้ผู้อ่านเข้าใจ scope ที่ขยายออกชัดขึ้น, (5) `angular/concepts/wireframe.md` วางใต้ Angular concepts เพราะ workspace ผูก UX/UI fundamentals คู่กับ Angular ไว้แล้ว (Vision ข้อ 2.6/Tech Stack) ยอมรับได้ในตอนนี้ แต่ถ้าอนาคตมี concept UX/UI ทั่วไปเพิ่มหลายหน้าอาจพิจารณาแยก hub ใหม่
